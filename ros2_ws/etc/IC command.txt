# pin 할당
led1 26, led2 16, led3 20, led4 21
sw1 05, sw2 06, sw3 13, sw4 19
buzzer 12

# mini project1 핀 할당
led red [18, 19, 13, 6]
led green [5, 11, 9, 10]
led blue [8, 17, 7, 25]
motor (21 예정)
lcd(sda : 2 ,scl : 3)

# 경주 1
uss1 = triger : 18, echo : 19
uss2 = triger : 7, echo : 8
uss3 = triger : 9, echo : 10

sudo pigpiod && cd "Desktop/test"
sudo pigpiod && cd "Desktop/Week 10"
sudo pigpiod && cd "Desktop/Week 10 test"

# 경로
cd Desktop/ledControl
sudo python3 main.py

// mortor
pwma 18, pwmb 25
ain2 27, ain1 22
bin2 24, bin1 23

// bluetooth
tx 14, rx 15


# raspberry pi ip:
192.168.137.227
192.168.137.141


# wifi
wifi name : wififorras7887
wifi ps : 999999999


# VNC 업데이트
sudo apt update && sudo apt upgrade
sudo apt install realvnc-vnc realvnc-vnc-viewer
 
Interface option -> vnc -> yes - > ok
Display option -> resolution -> 1600 x 1200 -> finish -> reboot

vncserver-virtual # 연결 안될때


# raspberry setting
sudo raspi-config // 셋팅창


# bluetooth
configuration -> interface -> serial port : enabled, serial console : disenabled -> reboot
ls -l /dev/serial0 // 장치 보기
bluetoothctl // 블루투스 설정
system-alias "my-name" // 이름 설정
discoverable on // 탐색 허용


# camera
setting -> interface option -> legacy camera -> yes -> reboot
sudo modprobe bcm2835-v4l2 // 카메라 킴
libcamera-hello / 카메라 연결 확인
sudo vcgencmd get_camera / 켜짐 확인
raspistill -o output.jpg / 사진 찍기
sudo apt-get install ./"file name"

sudo apt-get update && sudo apt-get upgrade

sudo apt-get install build-essential tk-dev libncurses5-dev libncursesw5-dev libreadline6-dev libdb5.3-dev libgdbm-dev libsqlite3-dev libssl-dev libbz2-dev libexpat1-dev liblzma-dev zlib1g-dev libffi-dev -y
sudo apt-get install libjpeg-dev libtiff5-dev libjasper-dev libpng-dev
sudo apt-get install libavcodec-dev libavformat-dev libswscale-dev libv4l-dev
sudo apt-get install libxvidcore-dev libx264-dev
sudo apt-get install libgtk2.0-dev
sudo apt-get install libatlas-base-dev gfortran 
sudo apt-get install python3-pip 
pip install numpy
sudo apt install python3-opencv

apt show python3-opencv

# webserver
sudo pip3 install fastapi
sudo pip3 install "uvicorn[standard]"

# 가상환경
python3 -m pip install virtualenv
python3 -m virtualenv env

source env/bin/activate # 접속
exec $SHELL # 해제

curl -L https://github.com/PINTO0309/Tensorflow-bin/releases/download/v2.5.0 -tensorflow-2.5.0-cp37-none-linux_armv7l.sh -o tensorflow-2.5.0-cp37-none-linux_armv7l.sh


sudo apt-get install build-essential tk-dev libncurses5-dev libncursesw5-dev libreadline6-dev libdb5.3-dev libgdbm-dev libsqlite3-dev libssl-dev libbz2-dev libexpat1-dev liblzma-dev zlib1g-dev libffi-dev -y && sudo apt-get install libjpeg-dev libtiff5-dev libjasper-dev libpng-dev && sudo apt-get install libavcodec-dev libavformat-dev libswscale-dev libv4l-dev && sudo apt-get install libxvidcore-dev libx264-dev && sudo apt-get install libgtk2.0-dev && sudo apt-get install libatlas-base-dev gfortran && sudo apt-get install python3-pip && pip install numpy && sudo apt install python3-opencv && apt show python3-opencv
